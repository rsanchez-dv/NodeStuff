import React from 'react';
import loaderSrc from '../../assets/loader.gif'

const Loader = props => (
    <div>
        <img alt="loader icon"
        alt="Loading Icon"
        src="{loaderSrc}"/>
    </div>
);

export default Loader;